<div class="request-summary table-responsive">
  <table class="table table-hover table-sm mb-4 caption-top">
    <caption><%= @caption %> <span class="badge rounded-pill text-bg-secondary"><%= count %></span></caption>
    <%- if @requests.present? %>
    <thead>
    <tr>
      <th scope="col" class="col-4"><%= t("account.requests.table_column_headings.title") %></th>
      <th scope="col" class="col-3"><%= t("account.requests.table_column_headings.location") %></th>
      <th scope="col" class="col-2"><%= t("account.requests.table_column_headings.request_date") %></th>
      <th scope="col" class="col-1"><span class="sr-only visually-hidden"><%= t("account.requests.table_column_headings.notes") %></span></th>
    </tr>
    </thead>
    <%- end %>
    <tbody>
    <%- if @requests.present? %>
      <%= render RequestTableRowComponent.with_collection(@requests) %>
    <%- elsif @requests.blank? %>
      <tr>
        <td><%= t("account.requests.no_request_message") %></td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>
