<h2><%= t("account.requests.heading") %></h2>
<div class="alert alert-info" role="alert">
  <p><%= t("account.requests.num_remaining", count: @requests[:numRequestsRemaining]) %></p>
</div>
<section class="request-summary table-responsive">
  <table class="table table-striped table-hover table-bordered">
    <caption><%= t("account.requests.table_headings.ready") %> (0)</caption>
    <thead class="bg-secondary text-light">
      <tr>
        <th><%= t("account.requests.table_column_headings.title") %></th>
        <th><%= t("account.requests.table_column_headings.location") %></th>
        <th><%= t("account.requests.table_column_headings.notes") %></th>
        <th><%= t("account.requests.table_column_headings.request_date") %></th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><%= link_to "test", "#" %></td>
        <td>test</td>
        <td>test</td>
        <td>test</td>
      </tr>
    </tbody>
  </table>
</section>

<section class="request-summary table-responsive">
  <table class="table table-striped table-hover table-bordered">
    <caption><%= t("account.requests.table_headings.requested") %> (0)</caption>
    <thead class="bg-secondary text-light">
    <tr>
      <th><%= t("account.requests.table_column_headings.title") %></th>
      <th><%= t("account.requests.table_column_headings.location") %></th>
      <th><%= t("account.requests.table_column_headings.notes") %></th>
      <th><%= t("account.requests.table_column_headings.request_date") %></th>
    </tr>
    </thead>
    <tbody>
    <tr>
      <td><%= link_to "test", "#" %></td>
      <td>test</td>
      <td>test</td>
      <td>test</td>
    </tr>
    </tbody>
  </table>
</section>

<section class="request-summary table-responsive">
  <table class="table table-striped table-hover table-bordered">
    <caption><%= t("account.requests.table_headings.not_available") %> (0)</caption>
    <thead class="bg-secondary text-light">
    <tr>
      <th><%= t("account.requests.table_column_headings.title") %></th>
      <th><%= t("account.requests.table_column_headings.location") %></th>
      <th><%= t("account.requests.table_column_headings.notes") %></th>
      <th><%= t("account.requests.table_column_headings.request_date") %></th>
    </tr>
    </thead>
    <tbody>
    <tr>
      <td><%= link_to "test", "#" %></td>
      <td>test</td>
      <td>test</td>
      <td>test</td>
    </tr>
    </tbody>
  </table>
</section>

<section class="request-summary table-responsive">
  <table class="table table-striped table-hover table-bordered">
    <caption><%= t("account.requests.table_headings.previous") %> (0)</caption>
    <thead class="bg-secondary text-light">
    <tr>
      <th><%= t("account.requests.table_column_headings.title") %></th>
      <th><%= t("account.requests.table_column_headings.location") %></th>
      <th><%= t("account.requests.table_column_headings.notes") %></th>
      <th><%= t("account.requests.table_column_headings.request_date") %></th>
    </tr>
    </thead>
    <tbody>
    <tr>
      <td><%= link_to "test", "#" %></td>
      <td>test</td>
      <td>test</td>
      <td>test</td>
    </tr>
    </tbody>
  </table>
</section>
