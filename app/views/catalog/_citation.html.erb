<%= render Blacklight::System::ModalComponent.new do |component| %>
  <% component.with_title { t('blacklight.tools.citation') } %>

  <%= render CitationComponent.with_collection(@documents) if @documents.present? %>
<% end %>
